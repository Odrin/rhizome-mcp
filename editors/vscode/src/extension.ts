import * as vscode from 'vscode';

export function activate(_context: vscode.ExtensionContext): void {
  // MCP server definitions are supplied by a later issue's provider registration.
}

export function deactivate(): void {}
